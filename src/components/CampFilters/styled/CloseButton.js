import styled from 'styled-components';

export default styled.button`
  padding: 0;
  border: none;
  margin: 0;
  background-color: transparent;

  transition: 0.5s;

  &:focus {
    outline: none;
  }
`;
